To run above script, 
1. add your input image to the folder and its location to the script
2. execute "python main.py"
3. you can play with 2 parameters- "num_of_iterations" and "num_components". 
For foreground-background extraction, num_components = 2
For semantic segmentation, num_components = # of visually identifiable clusters
